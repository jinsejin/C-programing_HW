#include <math.h>
//랜덤 숫자 출력하기. 사실은 랜덤은 아니지만~
rand() % 100 --> [0,99] // 0에서 99까지 랜덤 하게 나옴
int main (){


double sqrt(double x);   // declared in math.h

sqrt(u+v)* w * w;
sqrt((u-v)*(u-v)*(u-v));

sqrt(pow(u-v,3));

double log(double x);
double pow(double x, double y);
double abs(double x);

return 0;
}